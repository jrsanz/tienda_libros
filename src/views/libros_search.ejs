<%- include ('./partials/header_inicio') %>
    <title>Búsqueda de libros</title>
<%- include ('./partials/header_fin') %>

<!-- Inicio navbar -->
<%- include ('./partials/navbar') %>
<!-- Fin navbar -->

    <div class="container">
        <!-- Inicio sección 2 -->
        <hr>
        <h6 style="font-size: 18px">Resultados de la búsqueda</h6>
        <hr>
        <br>
        <div class="row">
        <% if (data != null) { %>
            <% for (var i = 0; i < data.length; i++) { %>
                    <div class="col-6 col-md-3 col-lg-2">
                        <div class="card text-center">
                            <a href="/details/<%= data[i].isbn %>"><img src="<%= data[i].imagen %>" class="card-img-top" alt="<%= data[i].nombre %>" style="height: 300px"></a>
                            <div class="card-body">
                                <div style="height: 80px">
                                    <h5 class="card-title"><a class="text-decoration-none" href="/details/<%= data[i].isbn %>" style="color: black"><%= data[i].nombre %></a></h5>
                                </div>
                                <p class="card-text"><%= data[i].autor %></p>
                                <p class="card-text"><strong>$<%= data[i].precio %></strong></p>
                                <a href="/details/<%= data[i].isbn %>" class="btn btn-success">Comprar <i class="bi bi-cart"></i></a>
                            </div>
                        </div>
                    </div>
            <% } %>
        <% } else { %>
            <h6 style="font-size: 18px">Lo sentimos, no encontramos los resultados de tu búsqueda.</h6>
            <p>Puedes intentarlo nuevamente por otro criterio de búsqueda, o si lo prefieres, puedes ponerte en contacto con nosotros.</p>
            <img class="img-fluid" src="/img/Sin%20resultados.jpg" style="width: 100%; height: 500px">
        <% } %>
        </div>
        <!-- Fin sección 1 -->
    </div>

    <br><br>

    <!-- Inicio footer -->
    <%- include ('./partials/footer') %>
    <!-- Fin footer -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>